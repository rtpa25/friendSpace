import styled from 'styled-components';

const MainWindowDiv = styled.div`
  background-color: #36393f;
  height: 100vh;
  flex: 10;
`;

const MainWindow = () => {
  return <MainWindowDiv></MainWindowDiv>;
};

export default MainWindow;
